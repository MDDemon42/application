const createOneAABAuthor = (
    id, last_name, first_name
) => {
    return (
        {
            id,
            last_name,
            first_name
        }
    )
};

export default createOneAABAuthor;